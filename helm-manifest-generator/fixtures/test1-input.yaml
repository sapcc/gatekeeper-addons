# NOTE: `test1-roundtripped.yaml` differs from this file only in field order,
# whitespace, and in having the autogenerated owner-info ConfigMap among its
# `items`.

metadata:
  name: tenso-seeds
  namespace: tenso
  status: deployed
  version: 73

values:
  global:
    region: qa-de-1

owner-info:
  helm-chart-url: https://example.com
  maintainers: Jane Doe, Max Mustermann
  service: bar
  support-group: foo

items:
  - apiVersion: v1
    kind: ConfigMap
    metadata:
      name: keks
    data:
      foo: bar
